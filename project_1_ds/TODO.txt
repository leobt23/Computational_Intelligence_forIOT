TestMe:

Pre processamento do dataset (Remover NANs, missing values, etc.)
Temos que tirar os outliers (disse o prof);


project_NN:

Verificar diferença entre teste e validação nos modelos;
Testar uma RNN (Não deve correr bem);


